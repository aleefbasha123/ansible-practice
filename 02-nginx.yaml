- name: installing nginx 
  hosts: web
  tasks:
  - name: Insatting nginx
    ansible.builtin.dnf:
      name: nginx
      state: latest
  - name: start and enable nginx
    ansible.builtin.service:
      name: started
      state: started
      enabled: yes
    
  